[package]
name = "ballast-platform-linux"
version = "0.1.0"
edition = "2024"

[dependencies]
ballast-core = { version = "0.1.0", path = "../ballast-core" }
color-eyre.workspace = true
nix = { version = "0.31.3", features = ["fs"] }
